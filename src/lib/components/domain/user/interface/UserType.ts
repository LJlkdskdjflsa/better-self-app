export interface UserType {
  id: number;
  name: string;
  alumni_listing_enabled: boolean;
  blog_creation_enabled: boolean;
  event_creation_enabled: boolean;
  college_specific_metrics_enabled: boolean;
}
